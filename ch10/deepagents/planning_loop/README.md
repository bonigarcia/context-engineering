# Planning loop

The harness follows a simple planning loop:

1. Select a small set of relevant repo/docs files.
2. Read each file and convert it into a compact note.
3. Reduce the notes into a narrow `context_state`.
4. Synthesize the final research brief from that reduced state.

That loop is the long-horizon behavior in miniature: the work is incremental, but the context stays bounded.
