# Human approval

This slice marks the checkpoint where a human would review the reduced context or the final brief before the next step.

Typical pause points:

- after file selection
- after note reduction
- before publishing the synthesized brief

The runnable harness does not pause automatically, but the README documents where approval would be inserted in a fuller workflow.
