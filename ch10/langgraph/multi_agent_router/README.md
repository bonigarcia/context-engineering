# LangGraph multi-agent router

This companion documents a router graph that sends work to specialized nodes and merges their outputs.

## What it demonstrates

- A routing node that picks the right specialist
- Separate agent nodes with narrow responsibilities
- Explicit merging of routed outputs into one result
