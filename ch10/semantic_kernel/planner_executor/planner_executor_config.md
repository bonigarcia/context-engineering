# Planner and executor config

- Use this folder as the local example workspace.
- Keep the request string in `semantic_kernel_planner_executor.py` aligned with `planner_executor_plan.md`.
- The demo uses `ProcessBuilder`, `KernelProcessStep`, `KernelProcessEvent`, and `start` with local deterministic step classes.
- No API key or external service is required for this example.
