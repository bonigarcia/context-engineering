# Planner and executor plan

Goal: turn `Organize a three-step plan for a small day-of-week reminder workflow.` into three easy-to-check steps and run them through the local Semantic Kernel process example.

Plan:
1. Read the request `Organize a three-step plan for a small day-of-week reminder workflow.` and build the three-step plan.
2. Pass the plan to the executor step in order.
3. Return the deterministic execution output for the same three steps.
