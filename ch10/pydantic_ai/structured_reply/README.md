# Structured reply

This example shows structured output without tools or external services.

## Requirements

- Python 3.10+
- Dependencies in `requirements.txt`

## Run

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python structured_reply.py
```
