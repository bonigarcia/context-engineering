# LangFlow

This chapter bucket shows LangFlow as a visual builder for agents and context flows.

## Examples

- `context_router`: how a visual flow routes and assembles context
- `agent_canvas`: how an agent is composed on a canvas
- `mcp_bridge`: how LangFlow can sit next to MCP-style tool connections
- `component_reuse`: how reusable components keep flows maintainable
