# n8n subworkflow boundary

This companion shows where a larger automation should be split into a subworkflow.

Use the boundary to keep the main flow readable and move reusable steps into a separate workflow.
