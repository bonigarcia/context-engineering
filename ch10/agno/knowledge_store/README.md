# Knowledge Store

This companion would show how explicit knowledge sources are attached to the agent so retrieval becomes part of the runtime context.

## Run

1. Install the dependencies from `requirements.txt`.
2. Set `OPENAI_API_KEY`.
3. Run `python knowledge_store.py`.

The agent reads the local example folder through `FileSystemKnowledge`.
