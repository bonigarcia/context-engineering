# AI SDK

This chapter slice shows a minimal Vercel AI SDK setup centered on `generateText()`.

## Included example

- `basic_text_generation/`: a runnable Node example using `MockLanguageModelV3` from `ai/test`, so it runs without API keys.

## Run the example

```bash
cd ch10/ai_sdk/basic_text_generation
npm install
npm start
```
