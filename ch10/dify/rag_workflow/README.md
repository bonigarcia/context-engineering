# RAG workflow

This companion shows Dify as a visual RAG pipeline: retrieve relevant passages, feed them into the prompt, and generate a grounded answer.

## What it demonstrates

- Retrieval as an explicit workflow step
- Narrow context assembly before generation
- Answer generation that stays tied to the retrieved material
