# Tool routing

This companion shows how Dify can route a request to the right tool node instead of forcing one prompt to handle every action.

## What it demonstrates

- Intent-based branching before execution
- Separate tool paths with narrow responsibilities
- A merge step that returns one final answer
